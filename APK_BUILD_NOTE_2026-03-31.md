APK build tracking note – 2026-03-31
====================================

- Firestore rules and indexes have been updated and deployed to project `boltlog`.
- Cloud Functions code and CI were adjusted to align with the new rules and runtime.
- This commit exists to trigger a fresh GitHub Actions Android APK build that includes all current backend rule changes.

